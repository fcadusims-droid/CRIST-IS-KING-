{
  "name": "redemptio",
  "version": "0.1.0",
  "private": true,
  "description": "Extensão MV3 que substitui conteúdo explícito por imagens e mensagens de fé.",
  "scripts": {
    "setup": "bash scripts/setup-vendor.sh"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^4.22.0",
    "nsfwjs": "^4.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
